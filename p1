#include<stdio.h>
int main()
{
    int n,sum=1,i;
    printf("enter the range of values yu want to add");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        sum=sum+i;
    }
    printf("sum of %d numbers is:%d",n,sum);
    return 0;
}
